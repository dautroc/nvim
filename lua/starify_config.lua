local u = require('utils')

u.g.startify_change_to_dir = 0
u.g.startify_session_dir = '~/.vim/session'
u.g.startify_session_persistence = 1
u.g.startify_session_number = 3
u.g.startify_session_sort = 1
